import { Gnb } from '@components/common/layout/gnb/Gnb';
import WModal from './WModal';

const WLoadingPage = (props: { mode?: string; isLoading?: boolean }) => {
  return <div>isLoading...</div>;
};

export default WLoadingPage;
